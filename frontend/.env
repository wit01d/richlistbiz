# =============================================================================
# PUBLIC CONFIGURATION - Safe to commit to version control
# =============================================================================
# This file contains non-sensitive defaults and configuration.
# Sensitive credentials go in .env.local (gitignored).

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
VITE_APP_NAME=RichList.biz
VITE_APP_URL=https://richlist.biz
VITE_API_URL=https://api.richlist.biz

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
VITE_ENABLE_SIMULATION=true
VITE_ENABLE_ADMIN_CONSOLE=true
VITE_ENABLE_3D_EFFECTS=true

# -----------------------------------------------------------------------------
# Business Rules (Public - these are disclosed to users)
# -----------------------------------------------------------------------------
VITE_DEPOSIT_AMOUNT=10
VITE_MAINTENANCE_FEE_PERCENT=10
# Successor system: Each recruit gets random seq 1-N, nomination when position=sequence (25% chance)
VITE_SUCCESSOR_SEQUENCE_MAX=4
VITE_MIN_WITHDRAWAL=100
VITE_WITHDRAWAL_COOLING_HOURS=48
VITE_REFERRAL_LINK_EXPIRY_DAYS=30
